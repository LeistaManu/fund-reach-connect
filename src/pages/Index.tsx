// Update this page (the content is just a fallback if you fail to update the page)

import AfricanDevFunds from '@/components/AfricanDevFunds';

const Index = () => {
  return <AfricanDevFunds />;
};

export default Index;
